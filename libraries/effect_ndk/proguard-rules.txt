# Proguard rules specific to the effect NDK module.

# This prevents the names of native methods from being obfuscated.
-keep,includedescriptorclasses class androidx.media3.effect.ndk.** {
    native <methods>;
}
